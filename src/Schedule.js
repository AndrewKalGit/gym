import React from 'react';

function Schedule(props) {
    return (
        <div>
            <h1 class="flex justify-center">
                Schedule.js npm install schedule //potentially 
            </h1>
        </div>
    );
}

export default Schedule;